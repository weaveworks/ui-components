machine:
  services:
    - docker
  node:
    version: 8.4.0
test:
  override:
    - make lint
    - make tests

deployment:
  release:
    branch: master
    commands:
      - make release
      - make docs
